// TODO(phase3): remove this file and replace all callers with real Clerk auth()

export const TEST_AGENCY_CLERK_ID  = 'test_agency_001'
export const TEST_CREATOR_CLERK_ID = 'test_creator_001'
export const TEST_BRAND_CLERK_ID   = 'test_brand_001'

// TODO(phase3): replace with const { userId } = await auth()
export const getAgencyClerkId  = () => TEST_AGENCY_CLERK_ID
// TODO(phase3): replace with const { userId } = await auth()
export const getCreatorClerkId = () => TEST_CREATOR_CLERK_ID
// TODO(phase3): replace with const { userId } = await auth()
export const getBrandClerkId   = () => TEST_BRAND_CLERK_ID
